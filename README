MessagePack for Lua
===================
Still writing...

Requirement
===========
MessagePack and liblua 5.1 is required.

Install
=======
Run ./configure && make && make install.

Usage
=====

Serialization
=============

require "msgpack"
p = msgpack.Packer()

-- pack accepts variable length arguments.
-- data is a string.
data = p:pack(1, 2, 3, "strings", {"a", "r", "r", "a", "y", "s"},
              {t = "a", b = "l", e = "s"; 1, 2, 3, 4})


Deserialization
===============
require "msgpack"

u = msgpack.Unpacker()
u:feed(data)

for v in u do
  -- v has a serialized data
end